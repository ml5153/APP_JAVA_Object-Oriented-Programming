# ☕ Cafeteria Object-Oriented Design (Step by Step)

> **"객체에게 일을 시켜라!"** 
> 
데이터를 가진 객체가 데이터를 직접 처리하는 **책임 주도 설계(Responsibility-Driven Design)**의 과정을 카페테리아 예제를 통해 단계별로 구현한 프로젝트입니다.

---

## 🎯 학습 목표
- **책임과 협력**: 객체 간의 적절한 역할 분담 이해
- **캡슐화(Encapsulation)**: 정보 은닉을 통한 데이터 무결성 보장
- **다형성(Polymorphism)**: 인터페이스를 통한 유연한 확장
- **의존성 주입(DI)**: 객체의 생성과 사용을 분리하여 결합도 낮추기

---

## 🚀 Step-by-Step Evolution

### Step 1. 절차지향적 설계 (남의 주머니를 뒤지는 손님)
- **상황**: 손님이 바리스타의 내부 데이터(`beans`)에 직접 접근하여 수정합니다.
- **문제점**: 바리스타의 자율성이 훼손되고, 내부 로직이 외부로 노출되어 유지보수가 어려워집니다.

### Step 2. 캡슐화와 메시징 (내 일은 내가 한다)
- **개선**: 바리스타의 원두 상태를 `private`으로 차단합니다.
- **핵심**: 손님은 바리스타에게 `makeCoffee`라는 **메시지**만 전달하고, 바리스타는 자신의 데이터를 스스로 처리합니다. (데이터를 가진 놈이 일을 해라!)

### Step 3. 연관 관계와 협력 (캐셔의 등장)
- **구조**: `Customer` ↔ `Cashier` ↔ `Barista`
- **의존성**: 캐셔는 바리스타와 협력하기 위해 필드로 바리스타를 가집니다. 생성자를 통해 관계를 맺는 **연관 관계**를 형성하며, `final` 키워드를 통해 불변성을 보장합니다.

### Step 4. 인터페이스와 다형성 (바리스타는 교체 가능하다)
- **개선**: `CoffeeMaker` 인터페이스를 도입합니다.
- **결과**: 캐셔는 이제 구체적인 `Barista` 클래스가 아닌 `CoffeeMaker` 인터페이스에 의존합니다. 사람이든 로봇이든 동일한 규약을 지킨다면 캐셔의 코드 변경 없이 커피를 만들 수 있습니다.

### Step 5. 조립은 공장에서 (DIP와 의존성 주입)
- **문제점**: `Main` 클래스에서 `new`를 남발하며 객체를 직접 조립하고 있습니다.
- **개선**: 객체를 조립하고 설정하는 전용 클래스인 `OrderContext`를 도입합니다.
- **결과**: **생성(OrderContext)**과 **사용(Main)**이 완벽히 분리되어, 사용하는 쪽에서는 구체적인 조립 과정을 몰라도 서비스를 이용할 수 있습니다.

---

## 🧐 생각 및 심화 고민
- **바리스타와 원두의 분리**: 바리스타가 직접 원두를 가지는 대신, `BeanStorage`라는 별도 클래스를 만들어 재고 관리 책임을 위임하면 더 좋지 않을까?
- **데이터의 타입화**: `Coffee` 결과물이나 메뉴명을 `String` 대신 `Enum`으로 관리하여 오타 방지 및 타입 안정성을 높여보자.

---

## 📚 참고 자료
- 홍순구 튜터님 달리기반 강의 내용 반영
- 조영호 저, **'객체지향의 사실과 오해'**
- [객체지향의 사실과 오해 후기 보러가기](https://velog.io/@ml5153/Book-%EA%B0%9D%EC%B2%B4%EC%A7%80%ED%96%A5%EC%9D%98-%EC%82%AC%EC%8B%A4%EA%B3%BC-%EC%98%A4%ED%95%B4)